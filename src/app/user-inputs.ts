export interface UserInputs {
    term: string;
    location: string;
    latitude: string;
    longitude: string;
    radius: string;
    price: string; //   /!\ format "1, 2, 3" et non "$, $$, $$$" 
}
